# Environment Configuration
GEMINI_API_KEY="your-gemini-api-key-here"
MODEL="gemini-2.5-flash"
STORE_PREFIX="univr"

# Optional: set to "true" to allow English in the chatbot (user chooses language)
# ALLOW_ENGLISH="true"

# Admin auth (login only; no registration)
# Default username: tesfayh. Override with ADMIN_USERNAME if needed.
# ADMIN_USERNAME="tesfayh"
# Bcrypt hash of admin password – generate with: python -c "import bcrypt; print(bcrypt.hashpw(b'your-password', bcrypt.gensalt()).decode())"
# ADMIN_PASSWORD_HASH="$2b$12$..."
# JWT secret for admin tokens (set in production)
JWT_SECRET="change-me-in-production"

# Optional settings
# APP_ENV="development"
# DEBUG="true"
